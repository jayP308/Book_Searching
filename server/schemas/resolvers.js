 const { User } = require('../models/User');
 const { signToken } = require('../utils/auth');

 const resolvers = () => {
    Query : {

    }

    Mutation: {

    }
 };

 module.exports = resolvers;